name: A workflow for my Hello World file
on: push

jobs:
  build:
    name: Hello world action
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: testing secrets 🔒
        shell: bash
        env:
          TEST_SECRET: ${{ secrets.TEST_SECRET }}
        run: echo "$TEST_SECRET" > ~/.testsecret
      - name: print secret
        run: cat ~/.testsecret
      - uses: ./action-a
        with:
          MY_NAME: "Mona"
